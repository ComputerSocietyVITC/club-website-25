---
const values = ["Empathy", "Development", "Innovation", "Community", "Creativity", "Growth"];
---

<div class="w-full py-8 bg-gradient-to-t from-black mt-auto md:mt-12 lg:mt-12">
  <div class="relative overflow-hidden whitespace-nowrap">
    <div class="inline-block animate-marquee">
      {values.map((value) => (
        <span class="text-white text-3xl uppercase tracking-wider px-16">
          {value}
        </span>
      ))}
    </div>
    <div class="inline-block animate-marquee">
      {values.map((value) => (
        <span class="text-white text-3xl uppercase tracking-wider px-16">
          {value}
        </span>
      ))}
    </div>
  </div>
</div>

<style>
  @keyframes marquee {
    0% {
      transform: translateX(0%);
    }
    100% {
      transform: translateX(-100%);
    }
  }

  .animate-marquee {
    animation: marquee 20s linear infinite;
  }

  .relative:hover .animate-marquee {
    animation-play-state: paused;
  }
</style>
